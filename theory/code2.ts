/*

TYPE ANNOTATIONS: passing type to particular variable.
1. explicit (e.g. in a function)
2. implicit (khud ho jayega)

they does not change the runtime behaviour of program.

Type Annotations not part of JS.
*/


// implicit type annotation 
let msg1 = "hello world msg1 \n";

 // explicit type annotation
let msg2:string = "hello world msg2";

console.log(msg1, msg2);
